.ipynb_checkpoints/
__pycache__/
.DS_Store

# Blind comparison pages are regenerated by harness/judge.py --render.
# responses/ is deliberately NOT ignored: archiving raw model output is the point.
harness/blind/

# Judging verdicts exported from the browser pages are transient input to
# judge.py --import; data/judgements.csv is the record that matters.
harness/verdicts.json
harness/verdicts_raw.json
